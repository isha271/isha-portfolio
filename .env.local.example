# MongoDB — contact form (optional, logs to console if missing)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/portfolio

# OpenAI — chatbot (optional, smart fallback built-in)
OPENAI_API_KEY=sk-...
